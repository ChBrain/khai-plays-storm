---
---

Flip `TS-047` (Der Schimmelreiter) in the Storm work-register to `status: staged`
with `play: ts047_der_schimmelreiter`, now that the play is live on `main` (#7), and
regenerate `REFERENCES.md` so the register's `Origin` table records the first staged
production against its TS id. Governance/docs only - the register and `REFERENCES.md`
ship no package content, so an empty changeset.
